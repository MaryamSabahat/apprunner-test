version: 1.0
runtime: python311
build:
  commands:
    build:
      - ls -R                  # DIAGNOSTIC: This lists all files so you can see extensions
      - pip install -r requirements.txt || pip install -r requirements
run:
  command: python app.py || python app
  network:
    port: 8080
